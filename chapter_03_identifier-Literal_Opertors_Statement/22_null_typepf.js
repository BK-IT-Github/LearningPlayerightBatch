// Null - "intentionally nothing
let selectedItem = null;
let searxhItem = null;
let user = null;

console.log(selectedItem);
console.log(searxhItem);
console.log(user);

// Undefined - "not yet assigned"
let declaredOnly;
console.log(declaredOnly);

// Equality comaparision
console.log(null == undefined);